# Governance examples

Filled `charter.md` and `ladder.md` from a real project, showing the evidence
discipline the outer loop depends on: every ticked box carries a command, a
result, a date, and an `auto`/`human` marker.

Absorbed from the `gate` skill when it was merged into boil (2026-08-28).
The blank templates live in `../../templates/`; the rules they follow are in
`../../references/outer-loop.md`.
